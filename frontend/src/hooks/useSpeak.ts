 // functie om een string om te zetten met TTS naar spraak
// function speak(text: string) {
//     const utterance = new SpeechSynthesisUtterance(text);
//     utterance.lang = 'nl-NL';
//     window.speechSynthesis.speak(utterance);
// }
// export function speak(text: string) {
//     const utterance = new SpeechSynthesisUtterance(text);
//     utterance.lang = 'nl-NL';
//     window.speechSynthesis.speak(utterance);
// }

/*
    <select>
    <option onfocus="speak(this.textContent)">Optie 1</option>
    <option onfocus="speak(this.textContent)">Optie 2</option>
    <option onfocus="speak(this.textContent)">Optie 3</option>
    </select>
    
    <script>
    function speak(text) {
    // Maak een nieuwe spraak instantie
    const utterance = new SpeechSynthesisUtterance(text);
    // Optioneel: stel de taal in
    utterance.lang = 'nl-NL';
    // Spreek de tekst uit
    window.speechSynthesis.speak(utterance);
    }
    </script>
*/
//function speak(text: string) {
//     const utterance = new SpeechSynthesisUtterance(text);
//     utterance.lang = 'nl-NL';
//     window.speechSynthesis.speak(utterance);
// }
// export function speak(text: string) {
//     const utterance = new SpeechSynthesisUtterance(text);
//     utterance.lang = 'nl-NL';
//     window.speechSynthesis.speak(utterance);
// }

/*
    <select>
    <option onfocus="speak(this.textContent)">Optie 1</option>
    <option onfocus="speak(this.textContent)">Optie 2</option>
    <option onfocus="speak(this.textContent)">Optie 3</option>
    </select>
    
    <script>
    function speak(text) {
    // Maak een nieuwe spraak instantie
    const utterance = new SpeechSynthesisUtterance(text);
    // Optioneel: stel de taal in
    utterance.lang = 'nl-NL';
    // Spreek de tekst uit
    window.speechSynthesis.speak(utterance);
    }
    </script>
*/